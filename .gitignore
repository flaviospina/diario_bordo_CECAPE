# Banco de dados local (criado automaticamente no primeiro uso)
data/*.sqlite
data/dbname.php

# Atestados enviados pelos professores (documento médico — nunca versionar)
data/atestados/

# Ajustes específicos de cada servidor
app/Config/config.local.php
