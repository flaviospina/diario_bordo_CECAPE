<?php
declare(strict_types=1);

defined('APP_RUNNING') or exit;

/**
 * Ajustes específicos deste servidor — copie este arquivo para
 * config.local.php (no mesmo diretório) e descomente o que precisar.
 * O config.local.php não é versionado no git.
 */

/*
 * BANCO MySQL (gerenciável pelo phpMyAdmin).
 * Crie o banco e o usuário no cPanel (MySQL® Databases), dê TODOS os
 * privilégios ao usuário no banco e preencha abaixo. Com estas três
 * primeiras linhas definidas, o sistema usa MySQL; sem elas, usa SQLite.
 *
 * Na primeira conexão com o banco MySQL vazio, os dados de uma instalação
 * SQLite existente em data/ são IMPORTADOS AUTOMATICAMENTE (contas,
 * atividades, etapas, pausas e descansos); o arquivo .sqlite é renomeado
 * para .importado-<data> e fica como backup.
 */
// define('DB_MYSQL_HOST', 'localhost');
// define('DB_MYSQL_NAME', 'usuario_diariobordo');   // nome com o prefixo do cPanel
// define('DB_MYSQL_USER', 'usuario_diario');        // idem
// define('DB_MYSQL_PASS', 'senha-do-banco');

/*
 * SQLite em VPS: guardar o banco FORA da raiz web, em um diretório que o
 * usuário do PHP (www-data etc.) possa escrever. Exemplo:
 *   sudo mkdir -p /var/lib/diariobordo
 *   sudo chown www-data:www-data /var/lib/diariobordo
 */
// define('DB_PATH_OVERRIDE', '/var/lib/diariobordo/diario.sqlite');

/*
 * Endereço público do sistema (usado nas meta tags de compartilhamento —
 * prévia do link no WhatsApp e redes sociais). Padrão:
 * https://cecapescs.com.br/diariobordo
 */
// define('APP_URL', 'https://outro-endereco.com.br/diariobordo');

/*
 * Caminho das logos institucionais (barra superior e login).
 * Padrão: https://cecapescs.com.br/logos
 * Use um caminho relativo se preferir hospedá-las junto da aplicação.
 */
// define('LOGO_BASE', 'assets/logos');
